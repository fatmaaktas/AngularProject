import { IsimFilterPipe } from './isim-filter.pipe';

describe('IsimFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IsimFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
